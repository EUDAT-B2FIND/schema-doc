.. _d:resourcetype:

ResourceType (O)
----------------
The type(s) of the resource (occurrence: 0-n). Free text values allowed.

**Examples**

dataset; text; survey data

Example
~~~~~~~
.. code-block:: xml
   :linenos:

  <resourceTypes>
    <resourceType>
     dataset
    </resourceType>
  </resourceTypes>